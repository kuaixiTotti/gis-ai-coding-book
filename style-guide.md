# 写作风格指南

本指南定义本书所有正文、代码示例、配套资源的风格规范。**写作时遇到拿不准的地方,以本指南为准**。

> 本指南是活的文档,发现规则不全或自相矛盾时,提 Issue 讨论后更新。

---

## 一、正文写作

### 1.1 整体风格

- **以"和读者对话"为基调**,不以"老师讲课"为基调
- **第二人称("你")多于第一人称复数("我们")**——本书直接对学生讲话
- **避免"显然"、"易得"、"众所周知"**——对你显然的对学生未必
- **每段不超过 5 句话**——超过就拆段
- **每节正文(不含代码)不超过 1500 字**——超过就拆节

### 1.2 中英文混排

- 中文与英文/数字之间加半角空格:`使用 Leaflet 加载 GeoJSON`
- 中文与中文标点之间不加空格:`你好,世界`
- 中文与英文标点之间也不加空格:`这是 first.`
- 引号:中文场景用 `"…"` 与 `'…'`,代码块内一律英文引号

### 1.3 术语处理

- **专业术语首次出现给出英文原文**:`空间连接(spatial join)`
- **同一术语全书统一**(见下文术语表)
- **避免生造词**——除非业内有共识

### 1.4 句式与遣词

避免以下写法:

| 避免 | 改为 |
|---|---|
| 我们将会看到 | 你会看到 |
| 不难发现 | (直接陈述) |
| 显而易见地 | (删除) |
| 通过……,我们可以…… | 用…… |
| 在这种情况下 | 这时 |
| 与……相对应的是 | (重写句子) |

### 1.5 段落标记符号

| 符号 | 含义 | 使用场景 |
|---|---|---|
| `> 💡` | 提示、加分项 | 锦上添花的内容 |
| `> 📌` | 重点、必记 | 不可忽略的关键点 |
| `> ⚠️` | 警告、踩坑 | 容易出错或有副作用 |
| `> 💬` | 旁注、思考 | 离题但有价值的延伸 |

> **不要叠用**。一段话只用一个符号。

---

## 二、代码示例

### 2.1 Python

- 用 `ruff` 默认规则格式化
- **变量名用英文,注释用中文**
- 字符串用双引号:`"Hello"`,例外是字符串内有双引号时用单引号
- 第 6 章起引入类型注解:`def process(data: gpd.GeoDataFrame) -> gpd.GeoDataFrame:`
- 行长不超过 88 字符
- import 顺序:标准库 → 第三方 → 本地,组间空行

```python
# ✓ 推荐
import geopandas as gpd
from shapely.geometry import Point

from .utils import to_crs_safe

# ✗ 避免:三组混在一起
from .utils import to_crs_safe
import geopandas as gpd
from shapely import Point
```

### 2.2 JavaScript / TypeScript

- 用 `prettier` 默认规则格式化
- 单引号字符串、分号结尾
- 函数组件 + Hooks
- 用 `const` 不用 `let`(除非真的需要重新赋值)
- 解构优于点访问

```javascript
// ✓ 推荐
const { name, category } = poi.properties;

// ✗ 避免
const name = poi.properties.name;
const category = poi.properties.category;
```

### 2.3 命令行

- 提示符统一用 `$`(Unix)。Windows PowerShell 特定场景才用 `>`,且明确说明
- 多行命令用反斜杠续行,不要用 Windows 的反引号
- 路径分隔符用 `/`,Windows 特定场景明确说明

### 2.4 代码注释的密度

| 章节阶段 | 注释密度 | 说明 |
|---|---|---|
| 第 1–4 章 | 高 | 几乎每段关键代码都有中文注释 |
| 第 5–9 章 | 中 | 复杂逻辑、非显然选择必须注释 |
| 第 10–12 章 | 低 | 假设读者已能读懂常见模式 |

注释**解释"为什么"**,不解释"是什么"。

```python
# ✓ 推荐:解释为什么
# 投影到 EPSG:3857 是为了让距离单位变为米
gdf_proj = gdf.to_crs(3857)

# ✗ 避免:复述代码
# 把 gdf 转换到 3857
gdf_proj = gdf.to_crs(3857)
```

### 2.5 错误示例的标记

讲解"反面教材"时,代码块上方明确标注:

```python
# ⚠️ 反例:不要这样写

def buggy_buffer(gdf):
    # 在地理坐标系上直接 buffer,单位是度,几乎没意义
    return gdf.buffer(0.001)
```

### 2.6 命名规范

- 项目目录:`chXX-kebab-case`(如 `ch04-leaflet-campus`)
- Python 文件:`snake_case.py`
- JS/TS 组件文件:`PascalCase.jsx`(如 `MapView.jsx`)
- JS/TS 工具文件:`camelCase.js`(如 `formatPoi.js`)
- 数据文件:`kebab-case.{ext}`(如 `beijing-poi.json`)

---

## 三、AI 对话记录格式

### 3.1 段内对话

短对话用引用块标注:

```markdown
> 👤 这段代码报错怎么办?
>
> 🤖 看起来是坐标系不一致,建议先用 `to_crs` 转换一下。
```

### 3.2 完整对话片段

完整对话存到独立的 `dialogues/<chapter>/<topic>.md` 文件,格式:

```markdown
# 对话标题

## 背景

[2-3 句话说明这次对话的目标和上下文]

## 完整对话

> 👤 [用户消息]

> 🤖 [AI 回复]

> 👤 [追问]

> 🤖 [回复]

## 关键时刻

- 🟢 第 N 轮:AI 主动指出我没考虑到的边界情况
- 🟡 第 N 轮:这里我应该追问但没问
- 🔴 第 N 轮:AI 给出了错误的 EPSG 代码,跑了才发现

## 复盘

[200-300 字反思:这次对话哪里做对了、哪里可以更好]
```

### 3.3 脱敏

对话中如包含个人信息(邮箱、token、真实姓名)、未公开数据,**必须脱敏**后再放入仓库。

---

## 四、图表与截图

### 4.1 文件命名

`figures/fig-chXX-NN-description.{png|svg|jpg}`

示例:`figures/fig-ch04-03-leaflet-mapbox-comparison.png`

### 4.2 屏幕截图要求

- 截图前**关闭浏览器书签栏、扩展图标、个人头像**等可能泄露信息的部分
- DevTools 截图只截关键面板,不截全屏
- 截图分辨率不低于 1920×1080,缩放后清晰可读
- 优先用 SVG 矢量图;PNG 用于真实截图;不要用 JPG(压缩失真)

### 4.3 图注

每张图片在 Markdown 中:

```markdown
![Leaflet 与 Mapbox 渲染效果对比](figures/fig-ch04-03-leaflet-mapbox-comparison.png)

**图 4-3** Leaflet(左)与 Mapbox GL(右)在同一份数据上的渲染效果对比。注意 Mapbox 的字体渲染更平滑,这是矢量瓦片的特征。
```

---

## 五、章节结构模板

每章正文遵循统一结构(微调可,大改要先讨论):

```markdown
# 第 X 章 章节标题

> **本章学习目标**
> - 目标 1
> - 目标 2
> - 目标 3
>
> **本章预计用时**:X–Y 小时

---

## X.1 开场:与读者建立联结的引子

[用具体场景、问题、对比开场,不要直接讲定义]

---

## X.2-X.N 主体内容

[每节 1500 字以内,按"动机 → 操作 → 解释 → 反思"的小循环展开]

---

## X.N 小结

[做了哪些事、关键认知是什么、下一章预告]

---

## 本章延伸阅读

[分类整理,每条加 1-2 句点评,给出阅读建议]

---

## 本章习题

**基础**(2-3 题:概念回顾、基础操作)

**进阶**(2-3 题:综合应用、轻度改造)

**挑战**(1-2 题:跨章节、研究生选作)
```

---

## 六、术语表(需要全书统一的写法)

| 推荐 | 不要使用 |
|---|---|
| AI 对话式编程 | AI 编程 / AI 辅助编程(本书特定语境) |
| Prompt(首次)/ 提示词 | 后续章节交替使用,但不能写成"提词" |
| 坐标系 | 投影系(除非真的指投影) |
| WGS84 / GCJ-02 / CGCS2000 | 大写,中间不加空格 |
| GeoJSON | Geo JSON / geojson(都不对) |
| 要素(feature) | 特征 / 元素 |
| 图层(layer) | 层(歧义) |
| 底图(basemap) | 背景图(歧义) |
| 仓库(repository) | 项目(指 Git 时) |
| 提交(commit) | 推送(那是 push) |

---

## 七、写作工作流

每章建议的写作工作流:

1. **周一**:确定主题、列提纲、跑通核心代码示例(2 小时)
2. **周二至周四**:每天 1 小时写作 + 整理 AI 对话记录(共 3-4 小时)
3. **周五**:配图、改稿、按本指南做格式检查、发布(2 小时)

**单章总投入应在 8-12 小时**。超出这个范围说明章节范围过大,需要拆分。

---

## 八、写作禁忌

以下做法**绝对避免**:

1. **不用 AI 对话截图代替代码**——截图会过时,代码要文字化
2. **不编造对话**——所有对话必须真实发生过
3. **不使用有版权争议的数据集**——见数据源清单
4. **不直接搬运英文教材的例子**——必须本土化
5. **不写"AI 万能"或"AI 无用"的极端立场**——本书的立场是务实合作
6. **不在正文中"未来章节会讲"超过 3 次**——埋的钩子要尽早回收
7. **不使用过去 6 个月内可能改变 API 的版本号**——除非测试过

---

## 九、本指南本身的更新

本指南随写作过程持续完善。每个版本号对应:
- v1.0:初始版本(2026 年)
- v1.x:小修小补
- v2.x:有结构性变化(比如新增章节模板)

更新本指南需要在 PR 中说明动机,且说服至少另一位维护者。
